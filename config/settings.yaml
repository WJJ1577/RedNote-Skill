# RedNote CLI configuration
client:
  proxy: None
  timeout: 30
  retry_interval: 5
  request_interval: 2

auth:
  cookies_file: "config/cookies.enc"

output:
  reports_dir: "data/reports"
  default_format: "html"
